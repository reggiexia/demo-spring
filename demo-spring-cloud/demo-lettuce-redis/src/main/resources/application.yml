spring:
  application:
    name: lettuce-redis
  redis:
    host: localhost
    lettuce:
      pool:
        max-active: 8 #最大连接数
        max-wait: -1 #默认-1 最大连接阻塞等待时间
        max-idle: 8 #最大空闲连接 默认8
        min-idle: 0 #最小空闲连接

server:
  port: 8080
